# UriPack: urienv

Thin Markpact — capabilities from [`urienv/manifest.yaml`](urienv/manifest.yaml).
Handlers: existing ``python://`` refs (code in `urienv/`, not duplicated here).

Generated by `urisys/scripts/generate_pack_markpacts.py`.

```yaml markpact:pack
apiVersion: urisys.io/v1
kind: UriPack
metadata:
  id: urienv
  version: '1'
  language: python
description: Controlled env:// runtime configuration and secrets.
schemes:
- env
capabilities:
- id: env-health
  uri: env://runtime/query/health
  kind: query
  operation: env.health
  handler: python://urienv.handlers:health
  side_effects: false
  approval: not_required
- id: env-list_vars
  uri: env://runtime/query/list
  kind: query
  operation: env.list_vars
  handler: python://urienv.handlers:list_vars
  side_effects: false
  approval: not_required
- id: env-startup_config
  uri: env://runtime/config/query/startup
  kind: query
  operation: env.startup_config
  handler: python://urienv.handlers:startup_config
  side_effects: false
  approval: not_required
- id: env-var_exists
  uri: env://runtime/var/{name}/query/exists
  kind: query
  operation: env.var_exists
  handler: python://urienv.handlers:var_exists
  side_effects: false
  approval: not_required
- id: env-var_value
  uri: env://runtime/var/{name}/query/value
  kind: query
  operation: env.var_value
  handler: python://urienv.handlers:var_value
  side_effects: false
  approval: not_required
- id: env-secret_masked
  uri: env://runtime/secret/{name}/query/masked
  kind: query
  operation: env.secret_masked
  handler: python://urienv.handlers:secret_masked
  side_effects: false
  approval: not_required
- id: env-secret_value
  uri: env://runtime/secret/{name}/query/value
  kind: query
  operation: env.secret_value
  handler: python://urienv.handlers:secret_value
  side_effects: true
  approval: required
- id: env-var_set
  uri: env://runtime/var/{name}/command/set
  kind: command
  operation: env.var_set
  handler: python://urienv.handlers:var_set
  side_effects: true
  approval: required
- id: env-var_unset
  uri: env://runtime/var/{name}/command/unset
  kind: command
  operation: env.var_unset
  handler: python://urienv.handlers:var_unset
  side_effects: true
  approval: required
policy:
  default: deny_mutations_without_approval
runtime:
  default_environment: real
  supports:
  - mock
  - local
  - docker
  expose:
  - pack
  - service
  - flow
  - interface
  - adapter
```

```yaml markpact:run
scheme: env
default: flow
modes:
- pack
- service
- flow
- interface
- adapter
service:
  port_hint: 8793
  path: /uri/call
flow:
  ids:
  - env-smoke
uses: []
adapter:
  call: POST /uri/call
  events: GET /events
```

```yaml markpact:tests
tests:
- id: env.health_query
  uri: env://runtime/query/health
  context:
    approved: true
    dry_run: true
    environment: mock
  expect:
    ok: true
    operation: env.health
- id: env.var_set_command
  uri: env://runtime/var/demo/command/set
  context:
    approved: true
    dry_run: true
    environment: mock
  expect:
    ok: true
    operation: env.var_set
  payload: {}
```

```yaml markpact:flow id=env-smoke
flow:
  id: env-smoke
  description: Smoke env:// routes (generated from manifest).
defaults:
  approved: true
  dry_run: true
do:
- env://runtime/query/health
- env://runtime/query/list
- env://runtime/config/query/startup
```

```markdown markpact:docs
## Unpack → `.markpact/`

```bash
cd urienv
export TELLMESH_ROOT=/home/tom/github/tellmesh
PACK=markpacts/urienv.markpact.md
urisys markpact run "$PACK" --as flow --approve --dry-run
urisys markpact run "$PACK" --as pack
urisys markpact run "$PACK" --as service --port 8793
urisys markpact run "$PACK" --as interface
urisys markpact run "$PACK" --as adapter
```

- Source manifest: `urienv/manifest.yaml`
- Requires: `pip install -e urienv` or `TELLMESH_ROOT` for python:// handlers
```
